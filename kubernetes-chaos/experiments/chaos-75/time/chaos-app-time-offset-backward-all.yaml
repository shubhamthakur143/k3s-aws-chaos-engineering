apiVersion: chaos-mesh.org/v1alpha1
kind: TimeChaos
metadata:
  name: chaos-app-time-offset-backward-all
  namespace: chaos-lab
  labels:
    chaos_name: chaos-app-time-offset-backward-all
spec:
  mode: all
  selector:
    namespaces:
      - chaos-lab
    labelSelectors:
      app: chaos-app
  timeOffset: "-60s"
  duration: "60s"
