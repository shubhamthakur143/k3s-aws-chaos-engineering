apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: chaos-app-pod-kill-01
  namespace: chaos-lab

spec:
  action: pod-kill
  mode: one
  duration: "30s"

  selector:
    namespaces:
      - chaos-lab
    labelSelectors:
      app: chaos-app
