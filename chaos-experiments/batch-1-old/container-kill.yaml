apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: chaos-app-container-kill
  namespace: chaos-lab
spec:
  action: container-kill
  mode: one
  duration: 30s
  selector:
    namespaces:
      - chaos-lab
    labelSelectors:
      app: chaos-app
