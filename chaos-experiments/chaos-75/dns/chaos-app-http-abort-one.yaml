apiVersion: chaos-mesh.org/v1alpha1
kind: HTTPChaos
metadata:
  name: chaos-app-http-abort-one
  namespace: chaos-lab
  labels:
    chaos_name: chaos-app-http-abort-one
spec:
  mode: one
  selector:
    namespaces:
      - chaos-lab
    labelSelectors:
      app: chaos-app
  target: Request
  port: 80
  abort: true
  duration: "60s"
