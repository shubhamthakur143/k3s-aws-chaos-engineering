apiVersion: chaos-mesh.org/v1alpha1
kind: DNSChaos
metadata:
  name: chaos-app-dns-random-all
  namespace: chaos-lab
  labels:
    chaos_name: chaos-app-dns-random-all
spec:
  action: random
  mode: all
  selector:
    namespaces:
      - chaos-lab
    labelSelectors:
      app: chaos-app
  patterns:
    - "mariadb"
  duration: "60s"
